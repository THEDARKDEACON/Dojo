sensor_fusion_node:
  ros__parameters:
    # Sensor timeout thresholds (seconds)
    lidar_timeout: 1.0
    camera_timeout: 1.0
    imu_timeout: 0.5
    odom_timeout: 0.5
    
    # Update rate (Hz)
    update_rate: 50.0
